import Up{{properCase name}} from './src/{{name}}.vue'

Up{{properCase name}}.install = Vue => {
  Vue.component(Up{{properCase name}}.name, Up{{properCase name}})
}

export default Up{{properCase name}}
